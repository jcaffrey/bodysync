extends template

block cssSheets
    link(rel='stylesheet', href='/stylesheets/patient-home.css')
    meta(name="viewport", content="width=device-width, initial-scale=1")
    script.
        window.onload = function () {
            loadPatientGeneralInfo();
        }

block content
    p.headerGrey Welcome back
    #generalInfo
    div.container-center
        button.buttonTab(onclick="location.href='/exercises';") Access Workouts
    p.headerGrey.patientMain You're on the road to recovery! Keep up the good work.
block footer
